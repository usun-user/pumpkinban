using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnPointLevelSelect : MonoBehaviour
{
    /*
    void Start()
    {
        transform.position = new Vector3(DataManager.Instance.levelSelectSpawnPosX, DataManager.Instance.levelSelectSpawnPosY, 0);
        GameManager.Instance.spawnPos = this.transform.position;
        GameManager.Instance.ContinueLevelSelectSetup();
    }

    public void SetSpawn()
    {
        DataManager.Instance.levelSelectSpawnPosX = GameManager.Instance.player.transform.position.x;
        DataManager.Instance.levelSelectSpawnPosY = GameManager.Instance.player.transform.position.y;
    }
    */
}
